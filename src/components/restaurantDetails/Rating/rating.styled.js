import { Grid } from '@mui/material';
import styled from 'styled-components';

export const StyledRating = styled(Grid)((props) => ({
  display: 'flex',
  alignItems: 'center',
  flexWrap: 'wrap',
}));
