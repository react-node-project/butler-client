import React from 'react';
import { StyledLayout } from './LeftNavBar.styled';

export type LeftNavBarProps = {};

const LeftNavBar = (props: LeftNavBarProps) => {
  return <StyledLayout>left Bar</StyledLayout>;
};

export default LeftNavBar;
